#!/usr/bin/env python

import sys
from datetime import datetime

while True:
    line = sys.stdin.readline()
    if line == '':
         break;
    print datetime.now().strftime("%H:%M:%S.%f")[:-3], line
    sys.stdout.flush()
